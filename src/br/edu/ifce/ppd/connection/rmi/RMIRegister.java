package br.edu.ifce.ppd.connection.rmi;

import java.rmi.Naming;

/**
 * Created by alcivanio on 07/05/17.
 */
public class RMIRegister {

    /*public static void registerServer(int id) {

        //choose the server name
        String SERVER_ONE_ID    = "//localhost/server_one";
        String SERVER_TWO_ID    = "//localhost/server_two";
        String serverId         = id == 0 ? SERVER_ONE_ID : SERVER_TWO_ID;

        //and starts it.
        try {
            RMIServer server = new RMIServer();
            Naming.rebind(serverId, server);
            System.out.println("Server with id " + serverId + " was registered.");

        } catch(Exception e) { e.printStackTrace(); }

    }*/

}
